import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  sessionTitle: {
    fontWeight: 'bold'
  },
  session: {
    flex: 1,
    padding: 10,
    borderColor: 'black',
    borderBottomWidth: 1
  },
  sessionRow: {
    flex: 1,
    flexDirection: 'row'
  }
})

export default styles;
